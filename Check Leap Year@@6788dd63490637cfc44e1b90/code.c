#include <stdio.h>
int main() {
    int year;
    if ((year%4==0 && year%100!=0) ||(yera%400==0)){
        printf("Leap Year"\n);
    }
    else{
        printf("Not a Leap Year"\n);
    }
    return 0;
}